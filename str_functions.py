# 1: len() is a function used to tell the length of the given string
a= "sajeel KHAN"
print(len(a))
print(a.endswith("HAN")) # Tells whether or not the string ends with the HAN characters
print(a.startswith("SA")) # Tells the start of the string with True or False
print(a.capitalize()) # only capitalize the first word of the string